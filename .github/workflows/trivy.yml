name: Trivy

on:
  workflow_call:

jobs:

  trivy:

    runs-on: self-hosted

    container:
      image: aquasec/trivy:latest

    steps:

      - uses: actions/checkout@v4

      - name: Terraform Security Scan
        run: |
          trivy config .